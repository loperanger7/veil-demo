// Veil iOS App — Entry Point
// This thin wrapper imports VeilUI and launches the app.

import SwiftUI
import VeilUI

@main
struct AppEntry: App {
    var body: some Scene {
        VeilApp().body
    }
}
